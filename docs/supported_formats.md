# Supported Formats

First-class: PDF, EPUB, DOCX, TXT, Markdown. Best-effort: HTML, RTF, ODT. Scanned PDF OCR is a roadmap item.

The knowledge tree renderer is fully local and writes SVG directly with the Python standard library. PNG is best-effort through optional `cairosvg`.
